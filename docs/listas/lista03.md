# Unidade 03

1. O que é SSH e como ele garante segurança na comunicação entre cliente e servidor?
1. Quais são as vantagens e desvantagens do uso de chaves SSH em comparação ao uso de senhas tradicionais?
1. Por que a chave privada é considerada o elemento mais sensível do processo de autenticação SSH? Como deve ser protegida?
1. Descreva o processo completo de autenticação por chaves SSH, desde a conexão inicial até a autorização final.
1. Em quais situações o comando ssh-copy-id é recomendado e como ele facilita a configuração de autenticação por chave?
1. O que é o ssh-agent e como ele contribui para segurança e praticidade no uso de chaves SSH?
1. Quais cuidados devem ser tomados antes de desabilitar o acesso por senha (PasswordAuthentication) no servidor SSH?
1. Explique por que o SSH é amplamente utilizado em tarefas de deploy e automação de servidores.
1. O que é o MariaDB/MySQL? Quais são as principais diferenças entre eles e por que o MariaDB é frequentemente utilizado em distribuições Linux como Debian?
1. Qual a função do comando mysql_secure_installation e por que ele é essencial após a instalação inicial do banco de dados?
1. Por que é importante criar um usuário administrador separado (como admin) em vez de usar o usuário root diretamente no MySQL/MariaDB?
1. Descreva o processo de criação de um banco de dados e de um usuário com permissões específicas para esse banco. Como essa prática aumenta a segurança?
1. Como acessar o terminal do MySQL como usuário root? Quais cuidados devem ser tomados ao acessar diretamente com root?
1. Qual a função do phpMyAdmin? Quais são as vantagens e riscos de utilizá-lo para administrar bancos de dados em servidores web?
1. Como instalar e configurar o phpMyAdmin com o Nginx e PHP-FPM? O que poderia acontecer se a configuração do PHP não fosse feita corretamente?
